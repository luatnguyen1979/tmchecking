export class User {
  constructor(private _id: string, private firstName: string, private lastName: string,
              private email: string, private password: string,
              private role: string, private telephone: string) {}

}
